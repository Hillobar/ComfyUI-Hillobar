# No dependencies beyond those ComfyUI already provides (torch, tqdm).
# Add pinned entries here as nodes are added, and mirror them into
# pyproject.toml's [project] dependencies for the Comfy Registry.
